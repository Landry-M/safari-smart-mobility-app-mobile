import 'package:flutter/material.dart';

class AuthDriverScreen extends StatelessWidget {
  const AuthDriverScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
